# Person A's stack. Copy to infra/.env and fill.
POSTGRES_PASSWORD=devonly
CLICKHOUSE_PASSWORD=devonly

# Handed to Person B for the LibreChat ClickHouse MCP connection. Read-only user,
# created by clickhouse/ddl/090_readonly_user.sql.
CH_READONLY_PASSWORD=changeme
